<UserControl x:Class="WPFProject.View.AdminMovieListUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFProject.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Background="WhiteSmoke">
            <ListBox FontSize="30" Background="DarkRed" Margin="-400,0,0,0" Width="400" Height="450" Name="AdminFilmsLstBx">
                
            </ListBox>
            <Button Command="{Binding RemoveMovieBtn}" Background="Red" Foreground="White" Name="RemoveMovieBtn" Content="Remove Film" Width="300" Height="50" FontSize="26" Margin="400,-200,0,0"></Button>
            <Button Command="{Binding BackBtn}" Background="Red" Name="BackBtn" Content="Back" Margin="400,-70,0,0" Width="200" Height="50" FontSize="30"></Button>
        </StackPanel>

    </Grid>
</UserControl>
